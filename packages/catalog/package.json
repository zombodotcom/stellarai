{
  "name": "@stellarai/catalog",
  "version": "0.0.0",
  "type": "module",
  "description": "Binary star-catalog container format and LOD octree index — written by the pipeline, read by the browser.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "build": "tsc -b" }
}
